**{app_name}**
Version {version} ({build_date})

Project Leader data-quality entry app for the OC_M deliverable.

This app runs disconnected from the database. All data exchange
happens via the pre-load snapshot (provided by the Data Manager) and
the submission file you save and hand back.

Edit this file (docs/about.md) any time to update this message —
no code changes needed. The {{app_name}}, {{version}}, and
{{build_date}} placeholders above are filled in automatically from
version.py; edit the surrounding wording freely, but leave those
three tokens as-is if you want the live version info to keep showing.

A copy of the SOP (OC_M SOP 11) ships with the app, but it should be
noted that this is NOT the authoritative version of SOP 11,
nor is this copy stored in this project's github repo.
